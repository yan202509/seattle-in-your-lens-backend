# This tells Spring to use a separate test database so you don't mess up your real one
spring.datasource.url=jdbc:postgresql://localhost:5432/seattle_test_db
spring.datasource.username=postgres


# This cleans the database every time you run the test so it starts fresh
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true